fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12
pillow>=10.0.0
numpy>=1.26.0
huggingface_hub>=0.25.0
scipy>=1.11.0
transformers>=4.40.0
einops>=0.7.0
kornia>=0.7.0
timm>=0.9.0
sam2 @ git+https://github.com/facebookresearch/sam2.git

# torch / torchvision are installed separately in the Dockerfile
# (CPU wheels for Dockerfile, bundled with the CUDA base image for Dockerfile.gpu)
# so the right build is picked for each target. If you're installing this
# outside Docker, install torch/torchvision yourself first — see README.
